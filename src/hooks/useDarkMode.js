import { useLocalStorage } from './useLocalStorage'

export const useDarkMode = () => {
    const [value, setValue] = useLocalStorage()

    // Dark mode is enabled

    // Setter function to toggle dark mode
}